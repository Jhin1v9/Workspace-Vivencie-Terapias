// Aura Presence - Componente de presença persistente

import React from 'react';
import { AuraUnified } from './AuraUnified';

export const AuraPresence: React.FC = () => {
  return <AuraUnified variant="orb" />;
};

export default AuraPresence;
